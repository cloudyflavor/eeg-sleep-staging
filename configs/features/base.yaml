# 에포크 npz 에서 특징 표를 만드는 설정
#
# 비교 실험의 축이 전부 여기 있다. 에포크 npz 를 다시 만들지 않고
# 이 파일만 바꿔 몇 번이고 다시 돌린다.
#
# wake 절단은 항상 적용한다 (설정 아님). 이 데이터는 집에서 24시간 연속
# 녹음한 것이라 절반 이상이 낮에 깨어 있는 구간인데, 실제 기기는 자기 전에
# 쓰고 아침에 벗는다. 자르는 쪽이 배포 조건에 가깝다.

features:
  epochs_dir: data/interim/epochs
  # 산출물은 <out_dir>/<설정해시>.parquet 이 된다. 설정과 파일이 1:1 로 묶여
  # 덮어쓸 일이 없고, 같은 설정을 다시 돌리면 건너뛴다.
  out_dir: data/features

  # none / causal / zerophase
  # 레퍼런스 셋이 셋 다 다르다 (DeepSleepNet 없음 / sleep-linear 인과 / YASA 영위상).
  # 셋 다 10분 지연 예산 안이므로 성능만 보고 고른다.
  filter: causal

  # none / expanding / posthoc
  # expanding = 누적 통계. lookahead 0 이라 실시간 가능하고, 밤이 깊어질수록 사후에 수렴.
  # posthoc = 밤 전체 통계 (YASA). 실시간 불가. 도달 가능한 상한선을 재는 용도.
  normalize: expanding

  # none / smooth / shift
  # none 은 기준선이라 반드시 재야 한다 — 시간 문맥이 얼마나 벌어주는지 알려면.
  # smooth = YASA (중심 7.5분 + 과거 2분) / shift = sleep-linear (±1, ±2 에포크)
  context: none

  # MMD · Esis. 기존 특징과 겹칠 여지가 커서 "넣는다" 가 아니라 "기여도를 잰다".
  distance_features: true

  # median / mean
  # 5초 조각 6개를 어떻게 합치는가. median 은 순간적인 잡음에 강하지만
  # 방추처럼 순간적인 신호도 함께 깎는다. 실측으로 방추 기여가 20% 작아진다.
  welch_average: median

  # 5초 조각 6개 각각의 대역 세기에서 최댓값과 최솟값을 남긴다.
  # 합치는 순간 사라지는 순간적인 변화를 붙잡는다.
  window_extremes: false

  # 수면방추를 사건으로 세어 개수와 지속시간을 남긴다.
  # 얕은 잠은 조각당 평균 4개, REM 은 0.1개다. 평균 세기로는 이 차이가 흐려진다.
  spindle_events: false

  # 두 채널의 파형이 대역마다 얼마나 닮았는지 남긴다.
  # 세기 비율만으로는 서파와 안구운동이 안 갈린다. 둘 다 이마가 더 세다.
  # 서파는 머리 전체가 같이 출렁여 닮고 이마에서만 나는 사건은 안 닮는다.
  channel_correlation: false
